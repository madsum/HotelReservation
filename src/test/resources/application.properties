# Test configuration
# Disable H2 console for tests
spring.h2.console.enabled=false

# Disable Kafka for integration tests
spring.kafka.listener.auto-startup=false

# Disable scheduler for integration tests
spring.task.scheduling.enabled=false

# Mock credit card service URL for tests
credit.card.payment.service.url=http://localhost:9999/mock-payment-status